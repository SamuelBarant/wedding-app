server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Sin esto, recargar /fotos o /admin en el navegador daría 404:
    # nginx no sabe de esas rutas, solo React Router (client-side).
    location / {
        try_files $uri /index.html;
    }
}
